# Debt Register

| ID | Severity | Area | Debt | Evidence | Recommended Next Action | Status |
| --- | --- | --- | --- | --- | --- | --- |
| D001 | High | Service boundary | `ai-orchestration-service` hosts too many domains | Phase 001 split controller classes; Phase 002 split internal query service ownership; Phase 005 selected next-governance-horizon modular monolith; Phase 008 documented per-domain transition-host exit criteria and readiness gates; Phase 009 documented report-specific readiness gates and blockers; Phase 010 documented market/data-ingest readiness gates and blockers; Phase 011 documented risk/strategy readiness gates and blockers, but the service host still contains multiple domains | Use the Phase 008 inventory plus the Phase 009 report, Phase 010 market/data-ingest and Phase 011 risk/strategy readiness artifacts before any extraction, route migration, projection split or permanence claim; select follow-up ownership, extraction or permanence work only through Window 0 and human approval | Open - Phase 011 refined risk/strategy readiness, transition-host debt remains |
| D002 | High | Contract | Non-task domains exposed under `/api/tasks/*` | risk, strategy, market, report, config APIs keep legacy `/api/tasks/*` paths in split controllers; Phase 006 froze the approved transitional inventory with focused backend contract tests; Phase 009 recorded the stable report URL/API inventory without migration; Phase 010 recorded the stable market/data-ingest URL/API inventory without migration; Phase 011 recorded stable risk/strategy URL/API inventory without migration | Later introduce domain URL groups only with explicit breaking-change approval; until then keep the Phase 006 inventory, Phase 009 report contract inventory, Phase 010 market contract inventory, Phase 011 risk/strategy contract inventory and guards in sync with any approved contract change | Open - report, market and risk/strategy contracts documented, legacy namespace remains transition debt |
| D003 | Medium | Authority | Future `research-workbench` or fallback metadata surfaces can be mistaken as SoT | Phase 003 added Java backend display-only contract notes and boundary tests that keep workbench out of command/projection authority and guard it from domain writes; Phase 004 added Python fallback provenance for planner, intent, financial, risk, report and market fallback paths; Phase 007 added frontend authority notes and a focused static guard for current workbench/fallback consumers; Phase 009 documented report fallback provenance and frontend display fields as non-authoritative; Phase 010 documented Python market context/fallback snapshots, frontend market state, source preview/diagnose, CNINFO proxy and mock/demo ingest as non-authoritative unless existing approved persistence writes selected data into authority objects; Phase 011 documented Python risk/strategy context, fallback provenance, generated risk/strategy events, frontend state and dashboard cards as non-authoritative unless existing approved projection or command paths persist selected data | Keep fallback/workbench/preview/display/generated-event metadata display-only or provenance-only, extend equivalent guardrails to future surfaces, and do not promote provenance into command or projection authority | Open - current known backend, Python, frontend, report, market and risk/strategy readiness surfaces mitigated |
| D004 | High | Read model | `TaskQueryServiceImpl` is too large and mixed | Phase 002 moved non-task read paths to internal domain query services and Window 3 approved after Fix Pass 1 | Keep boundary tests; do not reintroduce non-task methods into `TaskQueryServiceImpl` | Closed - Phase 002 completed |
| D005 | Medium | AI workflow | LangGraph workflow is linear | `workflow_builder.py` | Defer complex branching until contracts are stable | Open |
| D006 | Medium | Agent coverage | Missing planned agents | no event/industry/strategy/audit agent files | Add only after current workflow contract is frozen | Open |
| D007 | Medium | Config governance | Runtime config stored in JSON files | `ai-config/*.json` | Keep audited; decide DB/Nacos target later | Open |
| D008 | Medium | Security | Header-based demo auth | `UserContextFilter`, frontend localStorage role | Keep demo only; decide auth-service/JWT later | Open |
| D009 | Medium | Ingestion | No independent data-ingest-service | mock/source sync under ai-orchestration-service; Phase 010 documented mock/demo ingest, source sync, source preview/diagnose, CNINFO proxy, event source config and ingest history as transition responsibilities | Use Phase 010 readiness gates before any data-ingest-service split; keep mock/demo ingest non-production and define ingestion ownership only through Window 0 and human approval | Open - Phase 010 documented current market/data-ingest boundary, no split approved |
| D010 | Medium | Deployment | Docker compose lacks gateway/Nacos/Sentinel/service containers | compose has MySQL/Redis/Kafka/Zookeeper only | Defer until architecture boundary is stable | Open |
| D011 | Medium | Eval | Limited frontend tests; Python pytest unavailable in current env | Phase 004 added Python unittest fallback provenance coverage; Phase 007 added a focused frontend static guard and `npm run build` passed; `pytest` is unavailable | Add broader frontend/e2e coverage and install or replace pytest path when that becomes a phase goal | Open |
| D012 | Low | Namespace | Report/review/config APIs mixed with task namespace | `/api/tasks/...`; Phase 006 guards the approved mixed-namespace inventory | Do not add new mixed endpoints without approved phase handoff and contract inventory update | Open - guarded by Phase 006 |

## Debt Priority

Phase 001 completed the controller-surface part of D001 and reduced `TaskQueryController` drift.

Phase 002 completed the D004 internal read-model split and further reduced D001 by separating internal query service ownership inside `ai-orchestration-service`.

Phase 003 hardened D003 for the Java backend by documenting workbench display-only authority and adding source-level tests against backend command/projection use and domain writes.

Phase 004 hardened D003 for Python fallback auditability by preserving fallback provenance in existing result metadata and adding focused Python regression tests.

Phase 007 hardened D003 for current frontend consumer boundaries by documenting workbench/fallback provenance as display-only metadata and adding a focused static guard against command-authority usage.

Phase 006 hardened D002 by freezing the approved legacy non-task `/api/tasks/*` endpoint inventory with backend tests for path, method, owner, response envelope, request binding and permission behavior. The legacy namespace remains transition debt because Phase 006 preserved paths rather than migrating them.

Phase 005 addressed the immediate D001 policy question by choosing to continue `ai-orchestration-service` as a modular monolith for the next governance horizon. This does not close D001: the service remains a transition host, not final architecture, and extraction/permanence still require later human-approved phases.

Phase 008 completed the next D001 follow-through step by producing `docs/harness/12-transition-host-exit-criteria.md`, a static inventory of per-domain SoT, read-model, command, legacy route, guardrail, extraction blocker, exit criteria and readiness-gate facts for report, market, risk, strategy, audit, config and workbench responsibilities. It did not close D001 because no ownership move, extraction, route migration or permanence decision was approved.

Phase 009 completed the report-domain follow-through step by producing `docs/harness/13-report-boundary-readiness.md`, a static readiness artifact for report belongs, authority objects, read-model and command surfaces, versions, evidence, review audit, AI projection dependency, fallback provenance, frontend consumers, stable contracts, blockers and future gates. It did not close D001 or D002 because no ownership move, route migration, executable guardrail expansion or permanence decision was approved.

Phase 010 completed the market/data-ingest follow-through step by producing `docs/harness/14-market-data-ingest-boundary-readiness.md`, a static readiness artifact for market facts, relations, analysis, source config, ingest history, market read models and commands, mock/demo ingest, source sync/preview/diagnose, CNINFO proxy, market intelligence, Kafka `market.event.standardized`, auto-trigger context, frontend market consumers, Python market context/fallback provenance, stable contracts, blockers and future gates. It did not close D001, D002, D003, D007 or D009 because no ownership move, route migration, executable guardrail expansion, config-store migration, data-ingest split or permanence decision was approved.

Phase 011 completed the risk/strategy follow-through step by producing `docs/harness/15-risk-strategy-projection-boundary-readiness.md`, a static readiness artifact for risk warning facts, risk details, strategy signal facts, strategy factors, read models, strategy commands, `AiResultDomainProjectionService`, generated `risk.warning.generated` and `strategy.signal.generated` publication, frontend risk/strategy consumers, Python risk/strategy context and fallback provenance, stable contracts, blockers and future gates. It did not close D001, D002, D003, D007 or D008 because no ownership move, route migration, projection split, executable guardrail expansion, config-store migration, gateway/auth change or permanence decision was approved.

Next work should use the Phase 008 inventory and Phase 009/010/011 readiness artifacts to score bounded follow-up phases, such as config-store decisions, auth/gateway, legacy route migration, or report/market/risk/strategy extraction, projection, route, Kafka or config planning only if explicitly selected by Window 0 and approved by the user.

Do not begin large feature expansion until D001 follow-up decisions have explicit Window 0 selection and human approval, and any later D002 route migration is explicitly approved as a breaking-change phase.
