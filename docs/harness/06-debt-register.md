# Debt Register

| ID | Severity | Area | Debt | Evidence | Recommended Next Action | Status |
| --- | --- | --- | --- | --- | --- | --- |
| D001 | High | Service boundary | `ai-orchestration-service` hosts too many domains | Phase 001 split controller classes; Phase 002 split internal query service ownership; Phase 005 selected next-governance-horizon modular monolith; Phase 008 documented per-domain transition-host exit criteria and readiness gates; Phase 009 documented report-specific readiness gates and blockers; Phase 010 documented market/data-ingest readiness gates and blockers; Phase 011 documented risk/strategy readiness gates and blockers; Phase 012 documented config-store boundary and next-governance-horizon store decision; Phase 013 documented auth/gateway permission boundary and future readiness gates; Phase 014 scoped future production auth/gateway target prerequisites; Phase 015 selected future validator placement prerequisites; Phase 016 selected a future external IdP or enterprise directory issuer direction; Phase 017 selected future backend-owned application role authority direction; Phase 018 consolidated remaining auth, role, profile, service-propagation, audit, route and store gates, but the service host still contains multiple domains | Use the Phase 008 inventory plus the Phase 009 report, Phase 010 market/data-ingest, Phase 011 risk/strategy, Phase 012 config-store, Phase 013 permission, Phase 014 production auth/gateway target-scope, Phase 015 identity issuer/validator, Phase 016 identity issuer, Phase 017 role authority and Phase 018 remaining-governance closure artifacts before any extraction, route migration, projection/config split, gateway/auth implementation or permanence claim; select follow-up ownership, extraction, auth or permanence work only through Window 0 and human approval | Open - Phase 018 consolidated future prerequisites, transition-host debt remains |
| D002 | High | Contract | Non-task domains exposed under `/api/tasks/*` | risk, strategy, market, report, config APIs keep legacy `/api/tasks/*` paths in split controllers; Phase 006 froze the approved transitional inventory with focused backend contract tests; Phase 009 recorded the stable report URL/API inventory without migration; Phase 010 recorded the stable market/data-ingest URL/API inventory without migration; Phase 011 recorded stable risk/strategy URL/API inventory without migration; Phase 012 recorded stable config URL/API inventory without migration; Phase 013 recorded stable permission behavior, explicit permission checks, no-explicit-permission read surfaces and frontend header/route/action contracts without route migration; Phase 014 recorded future route migration dependencies on auth/gateway compatibility without route migration; Phase 015 recorded future route migration dependencies on identity validator placement without route migration; Phase 016 recorded future route migration dependencies on external issuer and gateway/JWT compatibility without route migration; Phase 017 recorded future route migration dependencies on backend-owned role authority, claim mapping and permission compatibility without route migration; Phase 018 recorded route migration and breaking-change gates without route migration | Later introduce domain URL groups only with explicit breaking-change approval; until then keep the Phase 006 inventory, Phase 009 report contract inventory, Phase 010 market contract inventory, Phase 011 risk/strategy contract inventory, Phase 012 config contract inventory, Phase 013 permission contract inventory, Phase 014 auth/gateway compatibility requirements, Phase 015 identity-validator compatibility requirements, Phase 016 issuer compatibility requirements, Phase 017 role-authority compatibility requirements and Phase 018 route migration gates in sync with any approved contract change | Open - compatibility contracts documented through Phase 018, legacy namespace remains transition debt |
| D003 | Medium | Authority | Future `research-workbench` or fallback metadata surfaces can be mistaken as SoT | Phase 003 added Java backend display-only contract notes and boundary tests that keep workbench out of command/projection authority and guard it from domain writes; Phase 004 added Python fallback provenance for planner, intent, financial, risk, report and market fallback paths; Phase 007 added frontend authority notes and a focused static guard for current workbench/fallback consumers; Phase 009 documented report fallback provenance and frontend display fields as non-authoritative; Phase 010 documented Python market context/fallback snapshots, frontend market state, source preview/diagnose, CNINFO proxy and mock/demo ingest as non-authoritative unless existing approved persistence writes selected data into authority objects; Phase 011 documented Python risk/strategy context, fallback provenance, generated risk/strategy events, frontend state and dashboard cards as non-authoritative unless existing approved projection or command paths persist selected data; Phase 012 documented frontend config defaults/localStorage, request headers, Python defaults/fallbacks, read models, audit rows and ingest history rows as non-authoritative relative to current config stores; Phase 013 documented frontend localStorage, route/menu/action gating, request headers, audit metadata and ingest metadata as non-authoritative relative to backend permission enforcement; Phase 014 documented service-to-service propagation and audit identity as future requirements, not authority from frontend, fallback, workbench or metadata; Phase 015 documented frontend localStorage, demo headers, `UserContext`, service principal context and audit identity as not production identity authority; Phase 016 documented external issuer, directory claims, service principal, audit identity and user profile facts as future-only, not current authority; Phase 017 documented external group/claim mapping, role metadata, frontend gating, service-to-service role handoff and role-audit facts as future-only or UI/runtime/provenance-only, not current role authority; Phase 018 consolidated identity/profile/role/audit/service metadata as future-only or runtime-only and kept D003 open | Keep fallback/workbench/preview/display/generated-event/config-display/permission-display/service-propagation/identity/issuer/profile/role metadata display-only, UI-only, requirement-only, runtime-only, future-only or provenance-only, extend equivalent guardrails to future surfaces, and do not promote provenance, display, cache, route/menu/action gating, audit metadata, propagation metadata, issuer metadata, profile metadata, role metadata or identity metadata into command, projection, config, permission, production identity or production role authority | Open - current known backend, Python, frontend, report, market, risk/strategy, config, permission, auth propagation, identity validator, issuer, role-authority and Phase 018 closure surfaces mitigated |
| D004 | High | Read model | `TaskQueryServiceImpl` is too large and mixed | Phase 002 moved non-task read paths to internal domain query services and Window 3 approved after Fix Pass 1 | Keep boundary tests; do not reintroduce non-task methods into `TaskQueryServiceImpl` | Closed - Phase 002 completed |
| D005 | Medium | AI workflow | LangGraph workflow is linear | `workflow_builder.py` | Defer complex branching until contracts are stable | Open |
| D006 | Medium | Agent coverage | Missing planned agents | no event/industry/strategy/audit agent files | Add only after current workflow contract is frozen | Open |
| D007 | Medium | Config governance | Runtime config stored in JSON files | `ai-config/*.json`; Phase 012 documented agent, workflow, model strategy, prompt template, event source, auto-trigger, role access, config audit and ingest history boundaries and selected JSON/prompt files as current runtime transition stores for the next governance horizon; Phase 013 kept `role-access-configs.json` as the current role/menu/permission config input under that JSON transition-store policy; Phase 014 kept `role-access-configs.json` as the current transition input while deferring backend-owned role authority migration; Phase 017 selected future backend-owned application role authority but kept `role-access-configs.json` as the current transition input; Phase 018 documented config-store and role-store migration readiness gates without approving migration | Keep current config mutations audited; use Phase 012, Phase 013, Phase 014, Phase 017 and Phase 018 readiness gates before any DB, Nacos, hybrid, audit/rollback, dual-read/dual-write, role-store or config-store migration target work; require Window 0 selection and human approval before migration | Open - Phase 018 documented future migration gates; JSON/prompt stores remain transition debt |
| D008 | Medium | Security | Header-based demo auth | `UserContextFilter`, frontend localStorage role; Phase 012 documented `role-access-configs.json`, request headers, frontend role utilities and `TaskRoleAccessService` as transition permission inputs/readers, not production auth architecture; Phase 013 produced `docs/harness/17-auth-gateway-permission-boundary.md`, classifying `X-User-Id` and `X-User-Role` as demo/runtime inputs, backend `requirePermission` checks as current enforcement points and frontend route/menu/action gating as UI affordance only; Phase 014 produced `docs/harness/18-production-auth-gateway-target-scope.md`, scoping future gateway/JWT ingress as the preferred target shape and backend-owned role authority as future-only while preserving demo headers for local/demo compatibility; Phase 015 produced `docs/harness/19-production-identity-issuer-validator-boundary.md`, selecting backend-owned ingress/gateway JWT validation as future validator placement while deferring concrete issuer and preserving demo headers; Phase 016 produced `docs/harness/20-production-identity-issuer-boundary.md`, selecting external IdP or enterprise directory as the preferred future issuer direction while keeping demo headers unchanged; Phase 017 produced `docs/harness/21-production-role-authority-boundary.md`, selecting future backend-owned application role authority while keeping demo headers and frontend role state unchanged; Phase 018 documented gateway/JWT prerequisites, demo-header compatibility policy shape, service principal semantics and audit identity gates without implementation | Keep demo only; use Phase 014 auth/gateway target-scope gates, Phase 015 identity issuer/validator gates, Phase 016 identity issuer gates, Phase 017 role-authority gates and Phase 018 remaining-governance gates before any external IdP integration, auth-service/JWT/gateway, identity issuer, identity validator, role authority implementation, role-store migration, service-to-service propagation or demo-header retirement implementation; require Window 0 selection and human approval before auth implementation | Open - Phase 018 consolidated future auth/role/profile prerequisites, demo auth remains |
| D009 | Medium | Ingestion | No independent data-ingest-service | mock/source sync under ai-orchestration-service; Phase 010 documented mock/demo ingest, source sync, source preview/diagnose, CNINFO proxy, event source config and ingest history as transition responsibilities | Use Phase 010 readiness gates before any data-ingest-service split; keep mock/demo ingest non-production and define ingestion ownership only through Window 0 and human approval | Open - Phase 010 documented current market/data-ingest boundary, no split approved |
| D010 | Medium | Deployment | Docker compose lacks gateway/Nacos/Sentinel/service containers | compose has MySQL/Redis/Kafka/Zookeeper only | Defer until architecture boundary is stable | Open |
| D011 | Medium | Eval | Limited frontend tests; Python pytest unavailable in current env | Phase 004 added Python unittest fallback provenance coverage; Phase 007 added a focused frontend static guard and `npm run build` passed; `pytest` is unavailable | Add broader frontend/e2e coverage and install or replace pytest path when that becomes a phase goal | Open |
| D012 | Low | Namespace | Report/review/config APIs mixed with task namespace | `/api/tasks/...`; Phase 006 guards the approved mixed-namespace inventory | Do not add new mixed endpoints without approved phase handoff and contract inventory update | Open - guarded by Phase 006 |

## Debt Priority

Phase 001 completed the controller-surface part of D001 and reduced `TaskQueryController` drift.

Phase 002 completed the D004 internal read-model split and further reduced D001 by separating internal query service ownership inside `ai-orchestration-service`.

Phase 003 hardened D003 for the Java backend by documenting workbench display-only authority and adding source-level tests against backend command/projection use and domain writes.

Phase 004 hardened D003 for Python fallback auditability by preserving fallback provenance in existing result metadata and adding focused Python regression tests.

Phase 007 hardened D003 for current frontend consumer boundaries by documenting workbench/fallback provenance as display-only metadata and adding a focused static guard against command-authority usage.

Phase 006 hardened D002 by freezing the approved legacy non-task `/api/tasks/*` endpoint inventory with backend tests for path, method, owner, response envelope, request binding and permission behavior. The legacy namespace remains transition debt because Phase 006 preserved paths rather than migrating them.

Phase 005 addressed the immediate D001 policy question by choosing to continue `ai-orchestration-service` as a modular monolith for the next governance horizon. This does not close D001: the service remains a transition host, not final architecture, and extraction/permanence still require later human-approved phases.

Phase 008 completed the next D001 follow-through step by producing `docs/harness/12-transition-host-exit-criteria.md`, a static inventory of per-domain SoT, read-model, command, legacy route, guardrail, extraction blocker, exit criteria and readiness-gate facts for report, market, risk, strategy, audit, config and workbench responsibilities. It did not close D001 because no ownership move, extraction, route migration or permanence decision was approved.

Phase 009 completed the report-domain follow-through step by producing `docs/harness/13-report-boundary-readiness.md`, a static readiness artifact for report belongs, authority objects, read-model and command surfaces, versions, evidence, review audit, AI projection dependency, fallback provenance, frontend consumers, stable contracts, blockers and future gates. It did not close D001 or D002 because no ownership move, route migration, executable guardrail expansion or permanence decision was approved.

Phase 010 completed the market/data-ingest follow-through step by producing `docs/harness/14-market-data-ingest-boundary-readiness.md`, a static readiness artifact for market facts, relations, analysis, source config, ingest history, market read models and commands, mock/demo ingest, source sync/preview/diagnose, CNINFO proxy, market intelligence, Kafka `market.event.standardized`, auto-trigger context, frontend market consumers, Python market context/fallback provenance, stable contracts, blockers and future gates. It did not close D001, D002, D003, D007 or D009 because no ownership move, route migration, executable guardrail expansion, config-store migration, data-ingest split or permanence decision was approved.

Phase 011 completed the risk/strategy follow-through step by producing `docs/harness/15-risk-strategy-projection-boundary-readiness.md`, a static readiness artifact for risk warning facts, risk details, strategy signal facts, strategy factors, read models, strategy commands, `AiResultDomainProjectionService`, generated `risk.warning.generated` and `strategy.signal.generated` publication, frontend risk/strategy consumers, Python risk/strategy context and fallback provenance, stable contracts, blockers and future gates. It did not close D001, D002, D003, D007 or D008 because no ownership move, route migration, projection split, executable guardrail expansion, config-store migration, gateway/auth change or permanence decision was approved.

Phase 012 completed the config-store decision-boundary follow-through step by producing `docs/harness/16-config-store-decision-boundary.md`, a static readiness artifact for agent config, workflow config, model strategy config, prompt templates, event source config, event auto-trigger config, role access config, config change audit, event ingest history, Java/Python readers, frontend config consumers, stable config contracts, deferred DB/Nacos/hybrid migration targets, blockers and future gates. It did not close D001, D002, D003, D007 or D008 because no ownership move, route migration, executable guardrail expansion, config-store migration, gateway/auth change or permanence decision was approved.

Phase 013 completed the auth/gateway permission boundary follow-through step by producing `docs/harness/17-auth-gateway-permission-boundary.md`, a static readiness artifact for request headers, backend request context, role-access config, backend permission services, explicit permission call sites, intentional no-explicit-permission read surfaces, `research-task-service` task-create permission checking, frontend role/header/menu/action consumers, stable permission contracts, deferred gateway/auth/JWT targets, blockers and future gates. It did not close D001, D002, D003, D007 or D008 because no ownership move, route migration, executable guardrail expansion, production auth implementation, role-store migration, config-store migration or permanence decision was approved.

Phase 014 completed the production auth/gateway target-scoping follow-through step by producing `docs/harness/18-production-auth-gateway-target-scope.md`, a static governance artifact that scopes future production identity authority through a backend-owned ingress/auth boundary, identifies gateway/JWT as the preferred future target shape, requires future production role authority to be backend-owned, preserves demo headers as local/demo compatibility inputs, keeps `role-access-configs.json` as the current transition role/menu/permission input, and records service-to-service propagation, audit identity, route migration and config/role-store dependencies. It did not close D001, D002, D003, D007 or D008 because no gateway/auth/JWT implementation, authority move, route migration, executable guardrail expansion, role-store migration, config-store migration, service extraction or permanence decision was approved.

Phase 015 completed the production identity issuer/validator follow-through step by producing `docs/harness/19-production-identity-issuer-validator-boundary.md`, a static governance artifact that selects backend-owned ingress/gateway JWT validation as the preferred future validator placement, defers the concrete identity issuer, user profile source and production role authority, preserves demo headers as local/demo compatibility inputs, and records token/session, service-principal, service-to-service identity handoff, audit identity, gateway compatibility, route migration compatibility and rollback readiness gates. It did not close D001, D002, D003, D007 or D008 because no gateway/auth/JWT implementation, issuer implementation, authority move, route migration, executable guardrail expansion, role-store migration, config-store migration, service extraction or permanence decision was approved.

Phase 016 completed the production identity issuer follow-through step by producing `docs/harness/20-production-identity-issuer-boundary.md`, a static governance artifact that selects external IdP or enterprise directory as the preferred future production identity issuer direction, keeps backend-owned ingress/gateway JWT validation as the preferred future validator placement, defers concrete vendor/product, token/session semantics, claim mapping, user profile source and production role authority, preserves demo headers as local/demo compatibility inputs, and records role-authority, gateway/JWT, service-principal, service-to-service, audit identity, route migration, config-store and role-store dependencies. It did not close D001, D002, D003, D007 or D008 because no external IdP integration, gateway/auth/JWT implementation, authority move, route migration, executable guardrail expansion, role-store migration, config-store migration, service extraction or permanence decision was approved.

Phase 017 completed the production role authority follow-through step by producing `docs/harness/21-production-role-authority-boundary.md`, a static governance artifact that selects backend-owned application role authority as the preferred future direction for production role assignment, role-permission mapping and menu mapping, keeps external IdP or enterprise directory groups/claims as future inputs only pending later approved mapping, preserves `role-access-configs.json` as the current transition role/menu/permission input, and records user profile, token/session, claim mapping, service-principal, service-to-service role handoff, audit identity, route migration, config-store and role-store dependencies. It did not close D001, D002, D003, D007 or D008 because no concrete role authority host, gateway/auth/JWT implementation, external group/claim mapping, authority move, route migration, executable guardrail expansion, role-store migration, config-store migration, service extraction or permanence decision was approved.

Phase 018 completed the consolidated remaining-governance closure step by producing `docs/harness/22-remaining-governance-closure.md`, a static governance artifact that gathers the remaining pre-implementation decisions and gates for concrete role authority host families, user profile source boundaries, service-to-service propagation, audit identity semantics, gateway/JWT prerequisites, demo-header compatibility policy shape, config-store and role-store migration readiness, route migration readiness and later implementation sequencing. It did not close D001, D002, D003, D007 or D008 because no gateway/auth/JWT implementation, external IdP integration, concrete role authority host, user profile source, service-to-service propagation implementation, audit field change, route migration, executable guardrail expansion, role-store migration, config-store migration, service extraction or permanence decision was approved.

Next work should use the Phase 008 inventory and Phase 009/010/011/012/013 readiness artifacts plus the Phase 014 target-scope artifact, Phase 015 identity issuer/validator artifact, Phase 016 identity issuer artifact, Phase 017 role authority artifact and Phase 018 remaining-governance closure artifact to score bounded follow-up phases, such as gateway/JWT implementation design, concrete production role authority host and mapping implementation planning, user profile source selection, service-to-service propagation and audit identity implementation design, legacy route migration, config-store or role-store migration planning, or report/market/risk/strategy extraction, projection, route, Kafka or config planning only if explicitly selected by Window 0 and approved by the user.

Do not begin large feature expansion until D001 follow-up decisions have explicit Window 0 selection and human approval, and any later D002 route migration is explicitly approved as a breaking-change phase.
